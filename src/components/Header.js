const headerStyle = {
  textAlign: 'center',
  fontSize: '3.2rem',
};

function Header() {
  return (
    <header style={headerStyle}>
      <h1>Quiz</h1>
    </header>
  );
}

export default Header;
